; Define kernel functions used in support vector machines

; Linear kernel
linear_kernel:
    ; ...

; Polynomial kernel
polynomial_kernel:
    ; ...

; Radial basis function (RBF) kernel
rbf_kernel:
    ; ...

; Additional kernel functions (e.g., sigmoid, hyperbolic tangent)